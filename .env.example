# Static checks and builds require no environment variables.
# Keep real Cloudflare credentials and other private values in ignored local files.
IGNAV_API_KEY="set-this-only-in-Cloudflare-secrets-or-.dev.vars"
